import { PopoverTemprefDirective } from './popover-tempref.directive';

describe('PopoverTemprefDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverTemprefDirective();
    expect(directive).toBeTruthy();
  });
});
